export class CompanyImage {
    id?: number;
    name?: string;
    type?: string;
    picByte?: Uint8Array;
    company?: Company;
  }
  
  export class Company {
    // Define the properties of the Company entity as needed
    // Example: id: number; name: string;
  }
  